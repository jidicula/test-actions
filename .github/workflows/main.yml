on: push

jobs:
  one:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os:
          - 'ubuntu-latest'
          - 'windows-latest'
          - 'macos-latest'
    steps:
      - name: Dump runner context
        env:
          RUNNER_CONTEXT: ${{ toJson(runner) }}
        run: echo "$RUNNER_CONTEXT"
      - name: Get runner OS
        env:
          RUNNER_OS: ${{ runner.os }}
        run: echo "$RUNNER_OS"
      - name: Create file with runner OS in name
        env:
          OS_FILENAME: 'spam-${{ runner.os }}.zip'
        run: |
          FILENAME="spam-${{ runner.os }}.zip"
          echo "OS_FILENAME=$FILENAME" >> $GITHUB_ENV
          touch "./$OS_FILENAME"
          touch blah.txt
      - name: List created file
        run: ls -l "./$OS_FILENAME"
